package hu.bme.aut.freelancer_spring.model.enums;

////////////////////////////////////
// The Status of the package, wether it's waiting
// for pickup (WAITING), already en route (INCAR), 
// or delivered to the destination (DELIVERED).
////////////////////////////////////
public enum Status {
    WAITING,
    INCAR,
    DELIVERED
}
